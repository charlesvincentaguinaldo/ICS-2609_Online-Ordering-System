export declare class PreparedStatement {
    private sqlString;
    private parameters;
    constructor(sqlString: string, parameters: Record<string, unknown>);
    prepare(): {
        preparedSql: string;
        preparedValues: any[];
    };
}
